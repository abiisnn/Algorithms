#include<bits/stdc++.h>
using namespace std;
#define optimizar_io ios_base::sync_with_stdio(0); cin.tie(0);
#define fore(i, a, b) for(int i = a; i < b; i++)
#define fori(i, a, b) for(int i = a; i <= b; i++)
#define pb push_back
#define mk make_pair
#define fi first
#define se second
#define endl '\n'
typedef long long int lli;

void addCuad(lli x, lli y, )
int main() {
	optimizar_io
	int q, n, f1, f2, f3, f4;
	lli X, Y;
	cin >> q;
	while(q--) {
		cin >> n;
		vector<set<pair<int, int>> cua;
		fore(i, 0, 4) {
			set<pair<int, int>> saux;
			cua.pb(aux);
		}
		fore(i, 0, n) {
			cin >> X >> Y >> f1 >> f2 >> f3 >> f4;
			addCuad(X, Y, cua);

		}
	}
}