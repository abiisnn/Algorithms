// https://codeforces.com/contest/1256/problem/A
#include<bits/stdc++.h>
typedef long long int lli;
using namespace std;

int main() {
	int tc;
	lli a, b, n, s;

	cin >> tc;
	while(tc--) {
		cin >> a >> b >> n >> s;
		if((( (a * n) % s ) * -1) )
	}
}